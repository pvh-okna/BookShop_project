export type StateType = {
    background?: string,
    color?: string,
}

export type PayloadType = {
    background: string,
    color: string
}

export type ThemeReducerType = {
    themeReducer: PayloadType
}

